# Build inputs only — everything here is either rebuilt in the image, secret,
# or host-specific.
node_modules
web/node_modules
dist
web/dist
.git
.gitignore
.env
*.env.local
.cache
.data
*.log
.DS_Store
.claude
.serena
tsconfig.tsbuildinfo
web/tsconfig.tsbuildinfo
# pnpm-lock.yaml and pnpm-workspace.yaml are deliberately NOT ignored:
# the build copies them before installing, which is what makes the
# dependency layer cacheable and the install reproducible.
Dockerfile
docker-compose.yml
README.md
